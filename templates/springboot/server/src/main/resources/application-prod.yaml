# 生产环境配置
example:
    knife4j: # 关闭knife4j
        production: true
    mysql:
        host: example-mysql
        password: ${MYSQL_PASSWORD}
    redis:
        host: example-redis
        password: ${REDIS_PASSWORD}
    rabbitmq:
        host: example-rabbitmq
        password: ${RABBITMQ_PASSWORD}
