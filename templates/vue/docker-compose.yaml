version: '3.8'

name: example-prefix

services:
    nginx:
        image: nginx:1.25-alpine
        container_name: example-prefix-nginx
        ports:
            - '80:80'
        volumes:
            - ./conf.d:/etc/nginx/conf.d:ro
            - ./dist:/usr/share/nginx/html/dist:ro
            - ./logs:/var/log/nginx
        environment:
            - TZ=Asia/Shanghai
        networks:
            - example-prefix

networks:
    example-prefix:
